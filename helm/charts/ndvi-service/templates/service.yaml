apiVersion: v1
kind: Service
metadata:
  name: {{ include "ndvi-service.fullname" . }}
  labels: {{- include "ndvi-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "ndvi-service.selectorLabels" . | nindent 4 }}
