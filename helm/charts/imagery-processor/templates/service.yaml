apiVersion: v1
kind: Service
metadata:
  name: {{ include "imagery-processor.fullname" . }}
  labels: {{- include "imagery-processor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "imagery-processor.selectorLabels" . | nindent 4 }}
