cloud:
  provider: gcp
  storageClass: pd-balanced
  ingressClass: gce

serviceAccount:
  annotations:
    iam.gke.io/gcp-service-account: farmpulse-storage-service@{{ .Values.gcp.project }}.iam.gserviceaccount.com
