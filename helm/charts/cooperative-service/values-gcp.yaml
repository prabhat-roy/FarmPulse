cloud:
  provider: gcp
  storageClass: pd-balanced
  ingressClass: gce

serviceAccount:
  annotations:
    iam.gke.io/gcp-service-account: farmpulse-cooperative-service@{{ .Values.gcp.project }}.iam.gserviceaccount.com
