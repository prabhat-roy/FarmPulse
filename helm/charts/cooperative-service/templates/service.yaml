apiVersion: v1
kind: Service
metadata:
  name: {{ include "cooperative-service.fullname" . }}
  labels: {{- include "cooperative-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "cooperative-service.selectorLabels" . | nindent 4 }}
