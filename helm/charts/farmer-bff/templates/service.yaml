apiVersion: v1
kind: Service
metadata:
  name: {{ include "farmer-bff.fullname" . }}
  labels: {{- include "farmer-bff.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "farmer-bff.selectorLabels" . | nindent 4 }}
