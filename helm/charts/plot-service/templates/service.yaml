apiVersion: v1
kind: Service
metadata:
  name: {{ include "plot-service.fullname" . }}
  labels: {{- include "plot-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "plot-service.selectorLabels" . | nindent 4 }}
