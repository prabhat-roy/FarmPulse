apiVersion: v1
kind: Service
metadata:
  name: {{ include "fertility-service.fullname" . }}
  labels: {{- include "fertility-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "fertility-service.selectorLabels" . | nindent 4 }}
