apiVersion: v1
kind: Service
metadata:
  name: {{ include "boundary-service.fullname" . }}
  labels: {{- include "boundary-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "boundary-service.selectorLabels" . | nindent 4 }}
