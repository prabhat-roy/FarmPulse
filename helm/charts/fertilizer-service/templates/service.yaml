apiVersion: v1
kind: Service
metadata:
  name: {{ include "fertilizer-service.fullname" . }}
  labels: {{- include "fertilizer-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "fertilizer-service.selectorLabels" . | nindent 4 }}
