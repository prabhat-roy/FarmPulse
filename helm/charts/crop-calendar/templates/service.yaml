apiVersion: v1
kind: Service
metadata:
  name: {{ include "crop-calendar.fullname" . }}
  labels: {{- include "crop-calendar.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "crop-calendar.selectorLabels" . | nindent 4 }}
