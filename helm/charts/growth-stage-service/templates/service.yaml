apiVersion: v1
kind: Service
metadata:
  name: {{ include "growth-stage-service.fullname" . }}
  labels: {{- include "growth-stage-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "growth-stage-service.selectorLabels" . | nindent 4 }}
