apiVersion: v1
kind: Service
metadata:
  name: {{ include "spray-recommendation.fullname" . }}
  labels: {{- include "spray-recommendation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "spray-recommendation.selectorLabels" . | nindent 4 }}
