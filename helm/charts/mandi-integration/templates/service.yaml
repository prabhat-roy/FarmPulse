apiVersion: v1
kind: Service
metadata:
  name: {{ include "mandi-integration.fullname" . }}
  labels: {{- include "mandi-integration.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "mandi-integration.selectorLabels" . | nindent 4 }}
