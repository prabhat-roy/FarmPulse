apiVersion: v1
kind: Service
metadata:
  name: {{ include "breeding-service.fullname" . }}
  labels: {{- include "breeding-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "breeding-service.selectorLabels" . | nindent 4 }}
