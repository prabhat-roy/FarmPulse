apiVersion: v1
kind: Service
metadata:
  name: {{ include "disease-alert.fullname" . }}
  labels: {{- include "disease-alert.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "disease-alert.selectorLabels" . | nindent 4 }}
