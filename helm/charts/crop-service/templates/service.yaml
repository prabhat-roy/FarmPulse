apiVersion: v1
kind: Service
metadata:
  name: {{ include "crop-service.fullname" . }}
  labels: {{- include "crop-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "crop-service.selectorLabels" . | nindent 4 }}
