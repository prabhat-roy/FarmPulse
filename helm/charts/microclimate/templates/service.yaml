apiVersion: v1
kind: Service
metadata:
  name: {{ include "microclimate.fullname" . }}
  labels: {{- include "microclimate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "microclimate.selectorLabels" . | nindent 4 }}
