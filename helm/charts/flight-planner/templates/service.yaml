apiVersion: v1
kind: Service
metadata:
  name: {{ include "flight-planner.fullname" . }}
  labels: {{- include "flight-planner.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "flight-planner.selectorLabels" . | nindent 4 }}
