apiVersion: v1
kind: Service
metadata:
  name: {{ include "field-service.fullname" . }}
  labels: {{- include "field-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "field-service.selectorLabels" . | nindent 4 }}
