apiVersion: v1
kind: Service
metadata:
  name: {{ include "herd-tracker.fullname" . }}
  labels: {{- include "herd-tracker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "herd-tracker.selectorLabels" . | nindent 4 }}
