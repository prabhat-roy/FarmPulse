apiVersion: v1
kind: Service
metadata:
  name: {{ include "variety-service.fullname" . }}
  labels: {{- include "variety-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "variety-service.selectorLabels" . | nindent 4 }}
