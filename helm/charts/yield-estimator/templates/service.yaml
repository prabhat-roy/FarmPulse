apiVersion: v1
kind: Service
metadata:
  name: {{ include "yield-estimator.fullname" . }}
  labels: {{- include "yield-estimator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "yield-estimator.selectorLabels" . | nindent 4 }}
