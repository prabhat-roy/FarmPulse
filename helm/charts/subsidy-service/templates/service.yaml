apiVersion: v1
kind: Service
metadata:
  name: {{ include "subsidy-service.fullname" . }}
  labels: {{- include "subsidy-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "subsidy-service.selectorLabels" . | nindent 4 }}
