apiVersion: v1
kind: Service
metadata:
  name: {{ include "pesticide-service.fullname" . }}
  labels: {{- include "pesticide-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "pesticide-service.selectorLabels" . | nindent 4 }}
