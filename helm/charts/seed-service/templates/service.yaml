apiVersion: v1
kind: Service
metadata:
  name: {{ include "seed-service.fullname" . }}
  labels: {{- include "seed-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "seed-service.selectorLabels" . | nindent 4 }}
