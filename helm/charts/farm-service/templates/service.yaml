apiVersion: v1
kind: Service
metadata:
  name: {{ include "farm-service.fullname" . }}
  labels: {{- include "farm-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "farm-service.selectorLabels" . | nindent 4 }}
