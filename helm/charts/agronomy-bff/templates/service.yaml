apiVersion: v1
kind: Service
metadata:
  name: {{ include "agronomy-bff.fullname" . }}
  labels: {{- include "agronomy-bff.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "agronomy-bff.selectorLabels" . | nindent 4 }}
