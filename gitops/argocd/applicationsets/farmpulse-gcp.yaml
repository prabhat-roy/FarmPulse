apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata: { name: farmpulse-gcp, namespace: argocd }
spec:
  generators:
    - git:
        repoURL: https://github.com/prabhat-roy/FarmPulse.git
        revision: HEAD
        directories: [{ path: helm/charts/* }]
  template:
    metadata:
      name: '{{path.basename}}-gcp'
      labels: { cloud: gcp, farmpulse.io/managed-by: argocd }
    spec:
      project: farmpulse
      source:
        repoURL: https://github.com/prabhat-roy/FarmPulse.git
        targetRevision: HEAD
        path: '{{path}}'
        helm:
          valueFiles: [values.yaml, values-gcp.yaml]
      destination:
        server: https://kubernetes.default.svc
        namespace: farmpulse
      syncPolicy:
        automated: { prune: true, selfHeal: true }
        syncOptions: [CreateNamespace=true, ServerSideApply=true]
