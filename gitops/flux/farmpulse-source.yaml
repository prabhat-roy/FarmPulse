apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata: { name: farmpulse, namespace: flux-system }
spec:
  interval: 1m
  url: https://github.com/prabhat-roy/FarmPulse.git
  ref: { branch: main }
