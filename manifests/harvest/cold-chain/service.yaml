apiVersion: v1
kind: Service
metadata:
  name: cold-chain
  namespace: farmpulse
  labels: { app.kubernetes.io/name: cold-chain, farmpulse.io/domain: harvest }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50202, targetPort: 50202 }]
  selector: { app.kubernetes.io/name: cold-chain }
