apiVersion: v1
kind: ConfigMap
metadata:
  name: cold-chain
  namespace: farmpulse
  labels: { app.kubernetes.io/name: cold-chain, farmpulse.io/domain: harvest }
data:
  LOG_LEVEL: info
  PORT: "50202"
