apiVersion: v1
kind: Service
metadata:
  name: storage-service
  namespace: farmpulse
  labels: { app.kubernetes.io/name: storage-service, farmpulse.io/domain: harvest }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50200, targetPort: 50200 }]
  selector: { app.kubernetes.io/name: storage-service }
