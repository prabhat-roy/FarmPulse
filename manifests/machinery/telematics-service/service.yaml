apiVersion: v1
kind: Service
metadata:
  name: telematics-service
  namespace: farmpulse
  labels: { app.kubernetes.io/name: telematics-service, farmpulse.io/domain: machinery }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50161, targetPort: 50161 }]
  selector: { app.kubernetes.io/name: telematics-service }
