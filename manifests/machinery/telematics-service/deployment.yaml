apiVersion: apps/v1
kind: Deployment
metadata:
  name: telematics-service
  namespace: farmpulse
  labels:
    app.kubernetes.io/name: telematics-service
    app.kubernetes.io/part-of: farmpulse
    farmpulse.io/domain: machinery
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: telematics-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: telematics-service, farmpulse.io/domain: machinery }
    spec:
      serviceAccountName: telematics-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: telematics-service
          image: harbor.farmpulse.internal/farmpulse/telematics-service:latest
          ports: [{ containerPort: 50161, name: http }]
          envFrom: [{ configMapRef: { name: telematics-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50161 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50161 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
