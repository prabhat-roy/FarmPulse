apiVersion: v1
kind: Service
metadata:
  name: maintenance-scheduler
  namespace: farmpulse
  labels: { app.kubernetes.io/name: maintenance-scheduler, farmpulse.io/domain: machinery }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50162, targetPort: 50162 }]
  selector: { app.kubernetes.io/name: maintenance-scheduler }
