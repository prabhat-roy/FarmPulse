apiVersion: v1
kind: ConfigMap
metadata:
  name: equipment-service
  namespace: farmpulse
  labels: { app.kubernetes.io/name: equipment-service, farmpulse.io/domain: machinery }
data:
  LOG_LEVEL: info
  PORT: "50160"
