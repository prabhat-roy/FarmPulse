apiVersion: v1
kind: ConfigMap
metadata:
  name: herd-tracker
  namespace: farmpulse
  labels: { app.kubernetes.io/name: herd-tracker, farmpulse.io/domain: livestock }
data:
  LOG_LEVEL: info
  PORT: "50140"
