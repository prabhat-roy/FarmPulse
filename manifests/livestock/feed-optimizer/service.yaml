apiVersion: v1
kind: Service
metadata:
  name: feed-optimizer
  namespace: farmpulse
  labels: { app.kubernetes.io/name: feed-optimizer, farmpulse.io/domain: livestock }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50142, targetPort: 50142 }]
  selector: { app.kubernetes.io/name: feed-optimizer }
