apiVersion: v1
kind: Service
metadata:
  name: breeding-service
  namespace: farmpulse
  labels: { app.kubernetes.io/name: breeding-service, farmpulse.io/domain: livestock }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50141, targetPort: 50141 }]
  selector: { app.kubernetes.io/name: breeding-service }
