apiVersion: v1
kind: Service
metadata:
  name: advisory-service
  namespace: farmpulse
  labels: { app.kubernetes.io/name: advisory-service, farmpulse.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50281, targetPort: 50281 }]
  selector: { app.kubernetes.io/name: advisory-service }
