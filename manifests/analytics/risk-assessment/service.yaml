apiVersion: v1
kind: Service
metadata:
  name: risk-assessment
  namespace: farmpulse
  labels: { app.kubernetes.io/name: risk-assessment, farmpulse.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50280, targetPort: 50280 }]
  selector: { app.kubernetes.io/name: risk-assessment }
