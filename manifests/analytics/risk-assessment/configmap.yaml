apiVersion: v1
kind: ConfigMap
metadata:
  name: risk-assessment
  namespace: farmpulse
  labels: { app.kubernetes.io/name: risk-assessment, farmpulse.io/domain: analytics }
data:
  LOG_LEVEL: info
  PORT: "50280"
