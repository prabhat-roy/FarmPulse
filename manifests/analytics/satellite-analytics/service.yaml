apiVersion: v1
kind: Service
metadata:
  name: satellite-analytics
  namespace: farmpulse
  labels: { app.kubernetes.io/name: satellite-analytics, farmpulse.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50282, targetPort: 50282 }]
  selector: { app.kubernetes.io/name: satellite-analytics }
