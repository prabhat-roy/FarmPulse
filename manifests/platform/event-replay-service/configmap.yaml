apiVersion: v1
kind: ConfigMap
metadata:
  name: event-replay-service
  namespace: farmpulse
  labels: { app.kubernetes.io/name: event-replay-service, farmpulse.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51009"
