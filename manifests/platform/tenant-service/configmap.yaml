apiVersion: v1
kind: ConfigMap
metadata:
  name: tenant-service
  namespace: farmpulse
  labels: { app.kubernetes.io/name: tenant-service, farmpulse.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51006"
