apiVersion: v1
kind: ConfigMap
metadata:
  name: api-key-service
  namespace: farmpulse
  labels: { app.kubernetes.io/name: api-key-service, farmpulse.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51003"
