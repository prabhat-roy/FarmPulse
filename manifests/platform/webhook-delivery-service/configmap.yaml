apiVersion: v1
kind: ConfigMap
metadata:
  name: webhook-delivery-service
  namespace: farmpulse
  labels: { app.kubernetes.io/name: webhook-delivery-service, farmpulse.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51004"
