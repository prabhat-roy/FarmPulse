apiVersion: v1
kind: Service
metadata:
  name: pest-service
  namespace: farmpulse
  labels: { app.kubernetes.io/name: pest-service, farmpulse.io/domain: pest-disease }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50100, targetPort: 50100 }]
  selector: { app.kubernetes.io/name: pest-service }
