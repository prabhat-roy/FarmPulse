apiVersion: v1
kind: Service
metadata:
  name: spray-recommendation
  namespace: farmpulse
  labels: { app.kubernetes.io/name: spray-recommendation, farmpulse.io/domain: pest-disease }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50102, targetPort: 50102 }]
  selector: { app.kubernetes.io/name: spray-recommendation }
