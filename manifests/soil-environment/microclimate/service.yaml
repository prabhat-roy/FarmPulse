apiVersion: v1
kind: Service
metadata:
  name: microclimate
  namespace: farmpulse
  labels: { app.kubernetes.io/name: microclimate, farmpulse.io/domain: soil-environment }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50062, targetPort: 50062 }]
  selector: { app.kubernetes.io/name: microclimate }
