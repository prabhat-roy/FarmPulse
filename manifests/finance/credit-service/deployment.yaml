apiVersion: apps/v1
kind: Deployment
metadata:
  name: credit-service
  namespace: farmpulse
  labels:
    app.kubernetes.io/name: credit-service
    app.kubernetes.io/part-of: farmpulse
    farmpulse.io/domain: finance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: credit-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: credit-service, farmpulse.io/domain: finance }
    spec:
      serviceAccountName: credit-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: credit-service
          image: harbor.farmpulse.internal/farmpulse/credit-service:latest
          ports: [{ containerPort: 50260, name: http }]
          envFrom: [{ configMapRef: { name: credit-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50260 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50260 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
