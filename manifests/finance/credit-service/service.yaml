apiVersion: v1
kind: Service
metadata:
  name: credit-service
  namespace: farmpulse
  labels: { app.kubernetes.io/name: credit-service, farmpulse.io/domain: finance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50260, targetPort: 50260 }]
  selector: { app.kubernetes.io/name: credit-service }
