apiVersion: v1
kind: Service
metadata:
  name: kcc-service
  namespace: farmpulse
  labels: { app.kubernetes.io/name: kcc-service, farmpulse.io/domain: finance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50263, targetPort: 50263 }]
  selector: { app.kubernetes.io/name: kcc-service }
