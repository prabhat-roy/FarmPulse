apiVersion: apps/v1
kind: Deployment
metadata:
  name: kcc-service
  namespace: farmpulse
  labels:
    app.kubernetes.io/name: kcc-service
    app.kubernetes.io/part-of: farmpulse
    farmpulse.io/domain: finance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: kcc-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: kcc-service, farmpulse.io/domain: finance }
    spec:
      serviceAccountName: kcc-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: kcc-service
          image: harbor.farmpulse.internal/farmpulse/kcc-service:latest
          ports: [{ containerPort: 50263, name: http }]
          envFrom: [{ configMapRef: { name: kcc-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50263 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50263 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
