apiVersion: v1
kind: Service
metadata:
  name: insurance-service
  namespace: farmpulse
  labels: { app.kubernetes.io/name: insurance-service, farmpulse.io/domain: finance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50261, targetPort: 50261 }]
  selector: { app.kubernetes.io/name: insurance-service }
