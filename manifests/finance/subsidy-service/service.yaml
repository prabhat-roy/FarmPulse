apiVersion: v1
kind: Service
metadata:
  name: subsidy-service
  namespace: farmpulse
  labels: { app.kubernetes.io/name: subsidy-service, farmpulse.io/domain: finance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50262, targetPort: 50262 }]
  selector: { app.kubernetes.io/name: subsidy-service }
