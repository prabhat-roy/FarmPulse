apiVersion: apps/v1
kind: Deployment
metadata:
  name: subsidy-service
  namespace: farmpulse
  labels:
    app.kubernetes.io/name: subsidy-service
    app.kubernetes.io/part-of: farmpulse
    farmpulse.io/domain: finance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: subsidy-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: subsidy-service, farmpulse.io/domain: finance }
    spec:
      serviceAccountName: subsidy-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: subsidy-service
          image: harbor.farmpulse.internal/farmpulse/subsidy-service:latest
          ports: [{ containerPort: 50262, name: http }]
          envFrom: [{ configMapRef: { name: subsidy-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50262 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50262 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
