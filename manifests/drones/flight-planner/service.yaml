apiVersion: v1
kind: Service
metadata:
  name: flight-planner
  namespace: farmpulse
  labels: { app.kubernetes.io/name: flight-planner, farmpulse.io/domain: drones }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50120, targetPort: 50120 }]
  selector: { app.kubernetes.io/name: flight-planner }
