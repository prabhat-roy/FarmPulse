apiVersion: v1
kind: Service
metadata:
  name: imagery-processor
  namespace: farmpulse
  labels: { app.kubernetes.io/name: imagery-processor, farmpulse.io/domain: drones }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50122, targetPort: 50122 }]
  selector: { app.kubernetes.io/name: imagery-processor }
