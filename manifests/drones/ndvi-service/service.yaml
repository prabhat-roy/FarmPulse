apiVersion: v1
kind: Service
metadata:
  name: ndvi-service
  namespace: farmpulse
  labels: { app.kubernetes.io/name: ndvi-service, farmpulse.io/domain: drones }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50121, targetPort: 50121 }]
  selector: { app.kubernetes.io/name: ndvi-service }
