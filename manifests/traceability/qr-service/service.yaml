apiVersion: v1
kind: Service
metadata:
  name: qr-service
  namespace: farmpulse
  labels: { app.kubernetes.io/name: qr-service, farmpulse.io/domain: traceability }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50221, targetPort: 50221 }]
  selector: { app.kubernetes.io/name: qr-service }
