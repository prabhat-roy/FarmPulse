apiVersion: v1
kind: ConfigMap
metadata:
  name: batch-service
  namespace: farmpulse
  labels: { app.kubernetes.io/name: batch-service, farmpulse.io/domain: traceability }
data:
  LOG_LEVEL: info
  PORT: "50220"
