apiVersion: v1
kind: Service
metadata:
  name: batch-service
  namespace: farmpulse
  labels: { app.kubernetes.io/name: batch-service, farmpulse.io/domain: traceability }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50220, targetPort: 50220 }]
  selector: { app.kubernetes.io/name: batch-service }
