apiVersion: apps/v1
kind: Deployment
metadata:
  name: batch-service
  namespace: farmpulse
  labels:
    app.kubernetes.io/name: batch-service
    app.kubernetes.io/part-of: farmpulse
    farmpulse.io/domain: traceability
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: batch-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: batch-service, farmpulse.io/domain: traceability }
    spec:
      serviceAccountName: batch-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: batch-service
          image: harbor.farmpulse.internal/farmpulse/batch-service:latest
          ports: [{ containerPort: 50220, name: http }]
          envFrom: [{ configMapRef: { name: batch-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50220 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50220 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
