apiVersion: v1
kind: ConfigMap
metadata:
  name: mfa-service
  namespace: farmpulse
  labels: { app.kubernetes.io/name: mfa-service, farmpulse.io/domain: identity }
data:
  LOG_LEVEL: info
  PORT: "50022"
