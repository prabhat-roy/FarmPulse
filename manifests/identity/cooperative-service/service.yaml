apiVersion: v1
kind: Service
metadata:
  name: cooperative-service
  namespace: farmpulse
  labels: { app.kubernetes.io/name: cooperative-service, farmpulse.io/domain: identity }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50021, targetPort: 50021 }]
  selector: { app.kubernetes.io/name: cooperative-service }
