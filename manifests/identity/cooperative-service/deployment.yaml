apiVersion: apps/v1
kind: Deployment
metadata:
  name: cooperative-service
  namespace: farmpulse
  labels:
    app.kubernetes.io/name: cooperative-service
    app.kubernetes.io/part-of: farmpulse
    farmpulse.io/domain: identity
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: cooperative-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: cooperative-service, farmpulse.io/domain: identity }
    spec:
      serviceAccountName: cooperative-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: cooperative-service
          image: harbor.farmpulse.internal/farmpulse/cooperative-service:latest
          ports: [{ containerPort: 50021, name: http }]
          envFrom: [{ configMapRef: { name: cooperative-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50021 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50021 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
