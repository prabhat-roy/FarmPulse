apiVersion: v1
kind: Service
metadata:
  name: fertilizer-service
  namespace: farmpulse
  labels: { app.kubernetes.io/name: fertilizer-service, farmpulse.io/domain: inputs }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50181, targetPort: 50181 }]
  selector: { app.kubernetes.io/name: fertilizer-service }
