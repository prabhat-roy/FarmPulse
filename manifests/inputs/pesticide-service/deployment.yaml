apiVersion: apps/v1
kind: Deployment
metadata:
  name: pesticide-service
  namespace: farmpulse
  labels:
    app.kubernetes.io/name: pesticide-service
    app.kubernetes.io/part-of: farmpulse
    farmpulse.io/domain: inputs
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: pesticide-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: pesticide-service, farmpulse.io/domain: inputs }
    spec:
      serviceAccountName: pesticide-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: pesticide-service
          image: harbor.farmpulse.internal/farmpulse/pesticide-service:latest
          ports: [{ containerPort: 50182, name: http }]
          envFrom: [{ configMapRef: { name: pesticide-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50182 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50182 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
