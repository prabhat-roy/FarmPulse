apiVersion: v1
kind: Service
metadata:
  name: pesticide-service
  namespace: farmpulse
  labels: { app.kubernetes.io/name: pesticide-service, farmpulse.io/domain: inputs }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50182, targetPort: 50182 }]
  selector: { app.kubernetes.io/name: pesticide-service }
