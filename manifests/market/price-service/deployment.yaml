apiVersion: apps/v1
kind: Deployment
metadata:
  name: price-service
  namespace: farmpulse
  labels:
    app.kubernetes.io/name: price-service
    app.kubernetes.io/part-of: farmpulse
    farmpulse.io/domain: market
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: price-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: price-service, farmpulse.io/domain: market }
    spec:
      serviceAccountName: price-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: price-service
          image: harbor.farmpulse.internal/farmpulse/price-service:latest
          ports: [{ containerPort: 50240, name: http }]
          envFrom: [{ configMapRef: { name: price-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50240 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50240 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
