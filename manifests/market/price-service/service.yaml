apiVersion: v1
kind: Service
metadata:
  name: price-service
  namespace: farmpulse
  labels: { app.kubernetes.io/name: price-service, farmpulse.io/domain: market }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50240, targetPort: 50240 }]
  selector: { app.kubernetes.io/name: price-service }
