apiVersion: apps/v1
kind: Deployment
metadata:
  name: auction-service
  namespace: farmpulse
  labels:
    app.kubernetes.io/name: auction-service
    app.kubernetes.io/part-of: farmpulse
    farmpulse.io/domain: market
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: auction-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: auction-service, farmpulse.io/domain: market }
    spec:
      serviceAccountName: auction-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: auction-service
          image: harbor.farmpulse.internal/farmpulse/auction-service:latest
          ports: [{ containerPort: 50241, name: http }]
          envFrom: [{ configMapRef: { name: auction-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50241 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50241 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
