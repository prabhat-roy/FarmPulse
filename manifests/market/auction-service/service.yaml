apiVersion: v1
kind: Service
metadata:
  name: auction-service
  namespace: farmpulse
  labels: { app.kubernetes.io/name: auction-service, farmpulse.io/domain: market }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50241, targetPort: 50241 }]
  selector: { app.kubernetes.io/name: auction-service }
