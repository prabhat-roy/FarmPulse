apiVersion: v1
kind: Service
metadata:
  name: mandi-integration
  namespace: farmpulse
  labels: { app.kubernetes.io/name: mandi-integration, farmpulse.io/domain: market }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50242, targetPort: 50242 }]
  selector: { app.kubernetes.io/name: mandi-integration }
