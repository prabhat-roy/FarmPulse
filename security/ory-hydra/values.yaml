# Ory Hydra — OAuth 2.0 / OIDC provider (lightweight, no UI)
image: { repository: oryd/hydra, tag: v2.2.0 }
hydra:
  config:
    urls:
      self: { issuer: https://hydra.farmpulse.internal/, public: https://hydra.farmpulse.internal/ }
    secrets: { system: ["__CHANGE_ME__"] }
