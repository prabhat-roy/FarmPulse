apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata: { name: farmpulse-vault-issuer }
spec:
  vault:
    server: https://vault.farmpulse.internal
    path: pki/sign/farmpulse-mesh
    auth:
      kubernetes:
        role: cert-manager
        mountPath: /v1/auth/kubernetes
        secretRef: { name: cert-manager-vault-token, key: token }
