global:
  novuVersion: 1.0.0
api: { ingress: { enabled: true, hosts: [{ host: novu-api.farmpulse.internal }] } }
web: { ingress: { enabled: true, hosts: [{ host: novu.farmpulse.internal }] } }
mongodb: { enabled: true }
redis:   { enabled: true }
