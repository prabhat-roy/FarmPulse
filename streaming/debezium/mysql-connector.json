{
  "name": "farmpulse-mysql-cdc",
  "config": {
    "connector.class": "io.debezium.connector.mysql.MySqlConnector",
    "database.hostname": "mysql.farmpulse.svc",
    "database.port": "3306",
    "database.user": "debezium",
    "database.password": "${file:/secrets/debezium-mysql.password:password}",
    "database.server.id": "184054",
    "topic.prefix": "farmpulse.mysql",
    "schema.history.internal.kafka.bootstrap.servers": "kafka.farmpulse.svc:9092",
    "schema.history.internal.kafka.topic": "farmpulse.mysql.schema-history"
  }
}
