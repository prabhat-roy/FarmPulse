apiVersion: batch/v1
kind: CronJob
metadata: { name: schemathesis, namespace: farmpulse-testing }
spec:
  schedule: "0 6 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: schemathesis
              image: schemathesis/schemathesis:stable
              args:
                - run
                - https://api.farmpulse.com/openapi.json
                - --checks=all
                - --hypothesis-deadline=10000
