apiVersion: temporal.io/v1beta1
kind: TemporalNamespace
metadata: { name: farmpulse, namespace: temporal-system }
spec:
  retentionPeriod: 30d
  archival:
    history:    { state: enabled, uri: s3://farmpulse-temporal-archive/history }
    visibility: { state: enabled, uri: s3://farmpulse-temporal-archive/visibility }
