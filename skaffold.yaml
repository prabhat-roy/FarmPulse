# Skaffold pipeline for FarmPulse
apiVersion: skaffold/v4beta11
kind: Config
metadata: { name: farmpulse }
build:
  artifacts:
    - { image: farmpulse/api-gateway, context: src/api-gateway }
    - { image: farmpulse/identity-service, context: src/identity-service }
  local: { push: false }
deploy:
  helm:
    releases:
      - { name: api-gateway, chartPath: helm/charts/api-gateway, namespace: farmpulse }
profiles:
  - name: prod
    deploy: { kubectl: { manifests: ["manifests/prod/*.yaml"] } }
