apiVersion: chaos-mesh.org/v1alpha1
kind: TimeChaos
metadata: { name: farmpulse-clock-skew, namespace: chaos-mesh }
spec:
  mode: one
  selector: { namespaces: [farmpulse] }
  timeOffset: "+500ms"
  duration: 3m
