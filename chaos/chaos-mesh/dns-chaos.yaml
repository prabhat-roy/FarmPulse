apiVersion: chaos-mesh.org/v1alpha1
kind: DNSChaos
metadata: { name: farmpulse-dns-error, namespace: chaos-mesh }
spec:
  action: error
  mode: all
  patterns: ["*.farmpulse.svc.cluster.local"]
  selector: { namespaces: [farmpulse] }
  duration: 2m
