name: farmpulse
description: FarmPulse — Pulumi alternative IaC (mirrors infra/terraform/{aws,gcp,azure})
runtime: { name: nodejs, options: { typescript: true } }
backend:
  url: s3://farmpulse-pulumi-state
config:
  farmpulse:env: prod
  farmpulse:cloud: aws
